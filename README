GIT

для начала работы входим в Git Bash
заходим в проект (для Саши cd Evil+Tab +Enter)
cd = change directory
cd .. на директорию выше

мы в директории проекта

после каждой операции git status, который показывает текущее состояние.
если всё чисто Branch Clean

если есть неиндексированные файлы(новые, только что созданные)
git add . 
. - означает всё, то есть "гит, добавь всё!"
затем можно коммититьi

обратная операция git rm имяфайла
rm = remove

основное понятие в гите коммит (commit) = пакет изменений
git commit -a -m "MyCommitName"
-a = all
-m = сообщение, название
git rm -f = forse remove
то есть заставить удалить

следующий шаг
git push origin master
push - толкнуть, толкнуть на сервер изменения(пакеты)

если мы позади версии на сервере, перед пушем следует скачать последнюю версию
git pull origin master
pull - тащить, стащить изменения с сервера


после каждой операции настоятельно рекомендуется делать git status

для гита важно понятие ветвей
есть master branch - локальная главная ветвь. на сервере есть - origin master branch главная на сервере